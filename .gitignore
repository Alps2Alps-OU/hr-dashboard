# ── Secrets & credentials — NEVER commit ──────────────────────────────────────
.env
.env.*
!.env.local.example
*.pem
*.key
**/service-account*.json
**/credentials*.json
**/*secret*.json

# ── Local database & employee data — contains PII, NEVER commit ───────────────
prisma/*.db
prisma/*.db-journal
*.sqlite
*.sqlite3

# ── Dependencies ──────────────────────────────────────────────────────────────
/node_modules
/.pnp
.pnp.js

# ── Next.js build output ──────────────────────────────────────────────────────
/.next/
/out/
/build
next-env.d.ts

# ── VDP / Coolify deployment secrets ──────────────────────────────────────────
coolify.env.*

# ── Misc ───────────────────────────────────────────